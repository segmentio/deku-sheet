machine:
  node:
    version: 4

dependencies:
  cache_directories:
    - node_modules
  override:
    - npm install
    - touch node_modules

deployment:
  publish:
    tag: /[0-9]+(\.[0-9]+)*(-.+)?/
    commands:
      - make
      - npm publish
